service:
  name: tennis-1point-notification

# Add the serverless-webpack plugin
plugins:
  - serverless-webpack

provider:
  name: aws
  runtime: nodejs8.10
  region: ap-northeast-1

functions:
  hello:
    handler: src/handler.sendNotification
    events:
      - http:
          method: get
          path: sendNotification
